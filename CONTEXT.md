# OMP 自动更新

本上下文定义本地 OMP 命令如何在不打断正常使用的情况下保持最新。

## 术语

**OMP 入口**：
用户用来启动 OMP 的命令。
_避免_：在指代用户命令时使用“更新器入口”。

**自动更新**：
在一次交互式调用继续执行前，检查并应用 OMP 已配置更新频道的预检操作。
_避免_：更新提醒。

**交互式调用**：
用户对 OMP 入口的一次执行，包括原始参数和正常进程生命周期。
_避免_：后台更新运行。

**更新频道**：
由 OMP 自身配置选择的 stable 或 canary 发布流。
_避免_：强制频道。

**当前版本**：
交互式调用开始时本地已安装的 OMP 版本；自动更新成功后，指新安装的版本。
_避免_：当更新频道或网络结果未知时使用“最新版本”。

**包装命令**：
执行自动更新预检，然后将同一次调用委托给原始 OMP 安装的 OMP 入口。
_避免_：提醒命令。

**更新截止时间**：
自动更新允许延迟交互式调用的最长时间，超过后回退到当前版本；默认值为 60 秒，并且可以配置。
